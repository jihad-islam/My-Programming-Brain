#include <bits/stdc++.h>
using namespace std;

int main()
{
    int base = 2;
    int exponent = 3;

    int result = int(pow(base * 1.0, exponent * 1.0));

    cout << result << endl;

    return 0;
}